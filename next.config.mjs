// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.ebayimg.com', 'www.fnp.qa'], // যেখান থেকে ইমেজ লোড করবেন সেটা এখানে যুক্ত করতে হবে
  },
};


export default nextConfig;
